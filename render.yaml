services:
  - type: web
    name: school-fee-frontend
    runtime: static
    rootDir: hub
    buildCommand: echo "Static hub ready"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
